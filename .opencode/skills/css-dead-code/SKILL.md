---
name: css-dead-code
description: CSS dead-code analysis for style.css. Which selectors are used, dead, or buggy in the 901-line application stylesheet.
---

# CSS Dead-Code Analysis (style.css)

## Mental Model
`style.css` is the main application stylesheet (901 lines). It contains application-specific layout/component styles plus AG Grid overrides. A significant portion is dead — leftover from earlier iterations (Tabulator, old lookup UI, etc.). One CSS variable bug exists.

## Coverage
**Documented:** Full dead-code audit of all 901 lines of style.css against all .ts/.tsx/.js/.jsx/.html files in SRC/
**Not yet documented:** None known
**Last extended:** 2026-06-14

## Key Findings

### Dead Selectors (no references in app code)

| Selector(s) | CSS Lines | Reason |
|---|---|---|
| `.sidebar-inner` | 79-83 | Orphaned layout div — never rendered |
| `.qb-card`, `.qb-title` | 237-244 | Replaced by `.card` / `.card-header` |
| `.join-divider`, `.join-divider-line`, `.join-grid` | 255-264 | Old join UI — never rendered |
| `.lookup-card`, `.lookup-row`, `.lookup-label`, `.lookup-cols`, `.chip-wrap` | 376-388 | Old lookup UI — replaced by `pl-lookup-*` classes |
| `.filter-row-wrap` | 330 | No wrapping element uses this class |
| `.pl-add-select` | 429-431 | Replaced by inline styles in stack-sheets.tsx |
| `.pl-row-badge` | 552-556 | No component uses this |
| `.modal-bg`, `.msub` | 685-689, 696 | Old modal — replaced by `.modal-backdrop`/`.modal-content` |
| `#resGrid`, `#prevGrid` + all `.tabulator-*` children | 654-665 | Tabulator library removed from project |
| `.cell-span-hidden`, `.cell-span-top`, `.merge-group-underline`, `.row-summary-subtotal .summary-box-*`, `.row-summary-grand .summary-box-*`, `.row-spacer`, `.row-spacer .ag-cell` | 801-863 | AG Grid cell classes no longer configured in grid.tsx |

### CSS Variable Bug
- **Line 765:** `#loadOverlay .lo-title { color: var(--fg); }` — `--fg` is NOT defined in `:root` (lines 3-15). Should be `var(--text)`.

### Used But Not Defined
- `.tab-opt--disabled` — used in `calc-builder.tsx` lines 295, 300 but no CSS rule exists for it
- `.filter-or-wrap` — used inline in `filter-list.tsx` line 119 but not a CSS class

## Critical Invariants
- The `chip-c0` through `chip-c15` classes ARE used (dynamically generated by `getTableColorClass()` in utils.ts, passed as `colorClass` prop to Chip component)
- All AG Grid theme overrides (`.ag-theme-balham-dark` block, lines 787-863) should be evaluated as a block — don't remove individual selector groups within it without checking AG Grid configuration
- When removing dead CSS, remove the ENTIRE Tabulator block (lines 654-665) as a unit

## Sources
- `style.css` (901 lines, full read)
- All `.ts`, `.tsx`, `.js`, `.jsx`, `.html` files under `SRC/` (grep searches for each CSS selector)
